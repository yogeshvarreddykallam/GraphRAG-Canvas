canvasapi>=3.2.0
weasyprint>=62.0
python-dotenv>=1.0.0
requests>=2.31.0
tqdm>=4.66.0

# Part 1: chunking + embeddings
# pypdf >=6.10.1 patches CVE-2026-41168 (DoS via crafted /Size or /N values)
pypdf>=6.10.1
sentence-transformers>=2.7.0
numpy>=1.26.0

# Part 2: FAISS retrieval + Ollama chat (requests already listed above)
faiss-cpu>=1.8.0

# Part 3: Gradio UI (6.0 changed the Chatbot API — pin to avoid 4.x/5.x issues)
gradio>=6.0.0

# ── Part 4: Knowledge Graph stack (GraphRAG extension) ─────
rdflib>=7.0.0
networkx>=3.3
spacy>=3.7.0
# After install run: python -m spacy download en_core_web_sm
pyvis>=0.3.2
